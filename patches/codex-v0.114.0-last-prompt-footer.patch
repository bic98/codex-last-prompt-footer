diff --git a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__status_line_setup__tests__setup_view_snapshot_uses_runtime_preview_values.snap b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__status_line_setup__tests__setup_view_snapshot_uses_runtime_preview_values.snap
index 000c7d898..3d8308773 100644
--- a/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__status_line_setup__tests__setup_view_snapshot_uses_runtime_preview_values.snap
+++ b/codex-rs/tui/src/bottom_pane/snapshots/codex_tui__bottom_pane__status_line_setup__tests__setup_view_snapshot_uses_runtime_preview_values.snap
@@ -12,11 +12,11 @@ expression: "render_lines(&view, 72)"
 › [x] model-name            Current model name                          
   [x] current-dir           Current working directory                   
   [x] git-branch            Current Git branch (omitted when unavaila…  
+  [x] last-prompt           Most recent submitted prompt preview (omi…  
   [ ] model-with-reasoning  Current model name with reasoning level     
   [ ] project-root          Project root directory (omitted when unav…  
   [ ] context-remaining     Percentage of context window remaining (o…  
   [ ] context-used          Percentage of context window used (omitte…  
-  [ ] five-hour-limit       Remaining usage on 5-hour usage limit (om…  
                                                                         
-  gpt-5-codex · ~/codex-rs · jif/statusline-preview                     
-  Use ↑↓ to navigate, ←→ to move, space to select, enter to confirm, esc
+  gpt-5-codex · ~/codex-rs · jif/statusline-preview · Q: fix footer tr… 
+  Use ↑↓ to navigate, ←→ to move, space to select, enter to confirm, esc
\ No newline at end of file
diff --git a/codex-rs/tui/src/bottom_pane/status_line_setup.rs b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
index 076ce5090..1d765a676 100644
--- a/codex-rs/tui/src/bottom_pane/status_line_setup.rs
+++ b/codex-rs/tui/src/bottom_pane/status_line_setup.rs
@@ -93,6 +93,9 @@ pub(crate) enum StatusLineItem {
     /// Full session UUID.
     SessionId,
 
+    /// Most recent submitted prompt preview.
+    LastPrompt,
+
     /// Whether Fast mode is currently active.
     FastMode,
 }
@@ -128,6 +131,7 @@ impl StatusLineItem {
             StatusLineItem::SessionId => {
                 "Current session identifier (omitted until session starts)"
             }
+            StatusLineItem::LastPrompt => "Most recent submitted prompt preview (omitted until you send one)",
             StatusLineItem::FastMode => "Whether Fast mode is currently active",
         }
     }
@@ -353,6 +357,7 @@ mod tests {
                 StatusLineItem::ModelName.to_string(),
                 StatusLineItem::CurrentDir.to_string(),
                 StatusLineItem::GitBranch.to_string(),
+                StatusLineItem::LastPrompt.to_string(),
             ]),
             StatusLinePreviewData::from_iter([
                 (StatusLineItem::ModelName, "gpt-5-codex".to_string()),
@@ -362,6 +367,7 @@ mod tests {
                     "jif/statusline-preview".to_string(),
                 ),
                 (StatusLineItem::WeeklyLimit, "weekly 82%".to_string()),
+                (StatusLineItem::LastPrompt, "Q: fix footer truncation".to_string()),
             ]),
             AppEventSender::new(tx_raw),
         );
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 7fdc294da..ee33314e4 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -29,6 +29,7 @@ use std::collections::BTreeMap;
 use std::collections::HashMap;
 use std::collections::HashSet;
 use std::collections::VecDeque;
+use std::env;
 use std::path::Path;
 use std::path::PathBuf;
 use std::sync::Arc;
@@ -376,6 +377,18 @@ fn is_standard_tool_call(parsed_cmd: &[ParsedCommand]) -> bool {
 const RATE_LIMIT_WARNING_THRESHOLDS: [f64; 3] = [75.0, 90.0, 95.0];
 const NUDGE_MODEL_SLUG: &str = "gpt-5.1-codex-mini";
 const RATE_LIMIT_SWITCH_PROMPT_THRESHOLD: f64 = 90.0;
+const STATUS_LINE_PROMPT_PREVIEW_GRAPHEMES: usize = 80;
+const STATUS_LINE_PROMPT_ENV_VAR: &str = "CODEX_LAST_PROMPT_FOOTER";
+
+fn status_line_prompt_enabled() -> bool {
+    match env::var(STATUS_LINE_PROMPT_ENV_VAR) {
+        Ok(value) => {
+            let normalized = value.trim().to_ascii_lowercase();
+            !matches!(normalized.as_str(), "0" | "false" | "off" | "no")
+        }
+        Err(_) => true,
+    }
+}
 
 #[derive(Default)]
 struct RateLimitWarningState {
@@ -691,6 +704,8 @@ pub(crate) struct ChatWidget {
     current_rollout_path: Option<PathBuf>,
     // Current working directory (if known)
     current_cwd: Option<PathBuf>,
+    // Most recent submitted user prompt shown alongside the footer status line.
+    last_submitted_prompt: Option<String>,
     // Runtime network proxy bind addresses from SessionConfigured.
     session_network_proxy: Option<codex_protocol::protocol::SessionNetworkProxyRuntime>,
     // Shared latch so we only warn once about invalid status-line item IDs.
@@ -1189,6 +1204,29 @@ impl ChatWidget {
         self.request_status_line_branch(cwd);
     }
 
+    fn set_last_submitted_prompt(&mut self, prompt: &str) {
+        let normalized = prompt.split_whitespace().collect::<Vec<_>>().join(" ");
+        let normalized = normalized.trim();
+        if normalized.is_empty() {
+            return;
+        }
+
+        self.last_submitted_prompt = Some(truncate_text(
+            normalized,
+            STATUS_LINE_PROMPT_PREVIEW_GRAPHEMES,
+        ));
+    }
+
+    fn status_line_prompt_label(&self) -> Option<String> {
+        if !status_line_prompt_enabled() {
+            return None;
+        }
+
+        self.last_submitted_prompt
+            .as_ref()
+            .map(|prompt| format!("Q: {prompt}"))
+    }
+
     fn collect_runtime_metrics_delta(&mut self) {
         if let Some(delta) = self.session_telemetry.runtime_metrics_summary() {
             self.apply_runtime_metrics_delta(delta);
@@ -3290,6 +3328,7 @@ impl ChatWidget {
             feedback_audience,
             current_rollout_path: None,
             current_cwd,
+            last_submitted_prompt: None,
             session_network_proxy: None,
             status_line_invalid_items_warned,
             status_line_branch: None,
@@ -3474,6 +3513,7 @@ impl ChatWidget {
             feedback_audience,
             current_rollout_path: None,
             current_cwd,
+            last_submitted_prompt: None,
             session_network_proxy: None,
             status_line_invalid_items_warned,
             status_line_branch: None,
@@ -3650,6 +3690,7 @@ impl ChatWidget {
             feedback_audience,
             current_rollout_path: None,
             current_cwd,
+            last_submitted_prompt: None,
             session_network_proxy: None,
             status_line_invalid_items_warned,
             status_line_branch: None,
@@ -5507,6 +5548,7 @@ impl ChatWidget {
                 format_tokens_compact(self.status_line_total_usage().output_tokens)
             )),
             StatusLineItem::SessionId => self.thread_id.map(|id| id.to_string()),
+            StatusLineItem::LastPrompt => self.status_line_prompt_label(),
             StatusLineItem::FastMode => Some(
                 if matches!(self.config.service_tier, Some(ServiceTier::Fast)) {
                     "Fast on".to_string()
diff --git a/codex-rs/tui/src/chatwidget/tests.rs b/codex-rs/tui/src/chatwidget/tests.rs
index 0896adc07..dd993ae8b 100644
--- a/codex-rs/tui/src/chatwidget/tests.rs
+++ b/codex-rs/tui/src/chatwidget/tests.rs
@@ -1888,6 +1888,7 @@ async fn make_chatwidget_manual(
         feedback_audience: FeedbackAudience::External,
         current_rollout_path: None,
         current_cwd: None,
+        last_submitted_prompt: None,
         session_network_proxy: None,
         status_line_invalid_items_warned: Arc::new(AtomicBool::new(false)),
         status_line_branch: None,
@@ -9801,6 +9802,34 @@ async fn status_line_model_with_reasoning_fast_footer_snapshot() {
     );
 }
 
+#[tokio::test]
+async fn status_line_appends_last_submitted_prompt_preview() {
+    let (mut chat, _rx, mut op_rx) = make_chatwidget_manual(Some("gpt-5.4")).await;
+    chat.thread_id = Some(ThreadId::new());
+    chat.config.tui_status_line = Some(vec!["model-name".to_string(), "last-prompt".to_string()]);
+    chat.refresh_status_line();
+    assert_eq!(status_line_text(&chat), Some("gpt-5.4".to_string()));
+
+    chat.submit_user_message_with_mode(
+        "check\nthis    footer prompt preview".to_string(),
+        chat.active_collaboration_mask
+            .clone()
+            .expect("expected collaboration mask"),
+    );
+
+    assert_matches!(
+        next_submit_op(&mut op_rx),
+        Op::UserTurn {
+            collaboration_mode: Some(_),
+            ..
+        }
+    );
+    assert_eq!(
+        status_line_text(&chat),
+        Some("gpt-5.4 · Q: check this footer prompt preview".to_string())
+    );
+}
+
 #[tokio::test]
 async fn stream_recovery_restores_previous_status_header() {
     let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(None).await;
